<?xml version="1.0" encoding="UTF-8" ?> <!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zhang.mapper.OrderMapper">
    <resultMap id="OrderResultMap" type="zhang.pojo.Order">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="pro_id" property="proId"/>
        <result column="or_num" property="orNum"/>
        <result column="totalprice" property="totalPrice"/>
        <association property="user" javaType="zhang.pojo.User">
                <!-- column:数据库表中字段名   property:pojo字段名-->
                <id column="id" property="id"/>
                <result column="user_name" property="userName"/>
                <result column="user_sex" property="userSex"/>
                <result column="user_age" property="userAge"/>
                <result column="user_tel" property="userTel"/>
        </association>
    </resultMap>
    <!--一个订单对应一个用户-->
    <select id="selectUserByOrderId" resultMap="OrderResultMap">
        SELECT o.*,u.* FROM order_info o,user_info u WHERE o.user_id=u.id AND o.id=#{id}
    </select>

    <resultMap id="OrderLazyResultMap" type="zhang.pojo.Order">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="pro_id" property="proId"/>
        <result column="or_num" property="orNum"/>
        <result column="totalprice" property="totalPrice"/>
        <association property="user" javaType="zhang.pojo.User" select="zhang.mapper.UserMapper.selectUserById" column="user_id">
            <!-- column:数据库表中字段名   property:pojo字段名-->
            <id column="id" property="id"/>
            <result column="user_name" property="userName"/>
            <result column="user_sex" property="userSex"/>
            <result column="user_age" property="userAge"/>
            <result column="user_tel" property="userTel"/>
        </association>
    </resultMap>
    <select id="orderLazyToUser" resultMap="OrderLazyResultMap">
        SELECT * FROM order_info WHERE id=#{id}
    </select>
</mapper>